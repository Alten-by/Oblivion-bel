# Задача: беларуская лакалізацыя Oblivion Remastered

Работай аўтаномна, пакуль не выканаеш усе пункты PROGRESS.md. Не задавай пытанняў: калі сумняваешся, выбірай варыянт, бліжэйшы да англійскага арыгінала, і запісвай спрэчнае ў QUESTIONS.md.

## Файлы

Кожны JSON — масіў запісаў з палямі:
- `key` — ідэнтыфікатар. НІКОЛІ не змяняць.
- `en` — англійскі арыгінал (галоўная крыніца сэнсу).
- `ru`, `pl` — рускі і польскі пераклады (дапаможныя, могуць быць з памылкамі).
- `bel`, `bel_alt` — два чарнавыя беларускія пераклады.
- `custom` — **выніковы пераклад. Гэта адзінае поле, якое ты запаўняеш.**

Правілы для `custom`:
- Пусты `custom` — трэба запоўніць.
- Ужо запоўнены `custom` зрабіў чалавек. НЕ змяняй яго. Калі бачыш там відавочную памылку (тэг, апячатка), запішы ў REVIEW.md: key, што не так, прапанова.
- Не змяняй іншыя палі, парадак запісаў і фармат файла (адступ 2 прабелы, UTF-8, `ensure_ascii=False`).

Працоўныя файлы:
- `same_bel_belalt_case_insensitive.json` — `bel` і `bel_alt` супадаюць (без уліку рэгістра).
- `different_bel_belalt_case_insensitive.json` — варыянты адрозніваюцца. Галоўны аб'ём работы.
- `ST_AltarDynamicTexts.json`, `ST_HardcodedContent.json` — тэксты інтэрфейсу і апісанні.
- `ST_AltarStaticTexts.json`, `ST_MissingEntries.json` — ужо запоўненыя, толькі праверка скрыптом.
- `ST_FullNames.json` — НЕ чапаць (гэта крыніца, з якой зроблены два файлы same/different).

Даведачныя папкі (толькі чытаць): `skyrim bel/`, `skyrim en/` — беларускі і англійскі тэксты Skyrim для тэрміналогіі.

## Эканомія (важна: бюджэт абмежаваны)

- Ніколі не чытай вялікія JSON цалкам і не выводзь іх у тэрмінал. Працуй праз скрыпты.
- Для кожнай порцыі: скрыпт выцягвае кампактны спіс (key, en, ru, bel, bel_alt, pl) толькі пустых запісаў → ты вырашаеш → скрыпт запісвае вынік у `custom`.
- Мэханічную работу (капіраванне, праверкі) рабі скрыптамі, а не ўручную.

## Крок 0. Падрыхтоўка

1. У `different_bel_belalt_case_insensitive.json` каля радка 175 ёсць сінтаксічная памылка (`"custom": "Агонь атранаха,` — няма закрываючай двукоссі). Выпраў толькі яе.
2. Напішы скрыпты ў `scripts/`:
   - `extract.py FILE START COUNT` — выводзіць кампактна (адзін запіс у радок, TSV ці JSONL) наступныя COUNT запісаў з пустым `custom`.
   - `apply.py FILE RESULTS` — запісвае `custom` з файла вынікаў (key → тэкст), не чапаючы нічога іншага.
   - `validate.py [FILES]` — правярае ўсе файлы:
     - JSON валідны, ключы і парадак не змяніліся, змянілася толькі `custom` (параўнанне з `git show HEAD:файл`);
     - тэгі і плэйсхолдэры ў `custom` супадаюць з `en`: `{Name}`, `<RT_...>`, `</>`, `<lf>` і да т.п. (колькасць і назвы);
     - няма рускіх літар `и щ ъ` і лацінкі па-за тэгамі (акрамя выпадкаў, калі `custom == en`, напр. тэхнічныя ID);
     - прабелы ў пачатку/канцы і канцавы знак прыпынку адпавядаюць `en`.
3. Запусці `validate.py` на ўсіх файлах, вынік запішы ў REVIEW.md.
4. Калі PROGRESS.md няма — ствары яго: чэклісты крокаў 0–4 і ўсіх порцый (з нумарамі запісаў).

## Крок 1. Глосарый

1. Спачатку вывучы фармат файлаў у `skyrim bel/` і `skyrim en/` (невялікімі кавалкамі).
2. Скрыптам знайдзі супадзенні: англійскія тэрміны з Oblivion (імёны, месцы, фракцыі, расы, школы магіі, навыкі, атрыбуты, прадметы, даэдры), якія ёсць у Skyrim, і іх беларускі пераклад там.
3. Вывучы ~2100 ужо запоўненых чалавекам `custom` у `different_...json`: якому варыянту (`bel` ці `bel_alt`) чалавек аддае перавагу і ў якіх выпадках. Коратка апішы гэтыя заканамернасці ў glossary.md (раздзел «Стыль»).
4. Ствары `glossary.md`: кампактная табліца `en | bel | крыніца (skyrim / custom / выбар)`. Толькі тэрміны, якія сустракаюцца больш за адзін раз або з'яўляюцца ўласнымі назвамі. Не больш за ~1500 радкоў.
5. Далей карыстайся ТОЛЬКІ glossary.md, не перачытвай папкі Skyrim.

## Крок 2. `same_bel_belalt_case_insensitive.json`

Скрыптам: для пустых `custom` ставіць `bel` (калі `bel` і `bel_alt` адрозніваюцца толькі рэгістрам — выбіраць варыянт па правілах рэгістра ніжэй). Потым скрыптам праверыць па глосарыі і `validate.py`; толькі падазроныя запісы глядзець уручную.

## Крок 3. `different_bel_belalt_case_insensitive.json`

Порцыі па ~300 пустых запісаў. Для кожнага запісу:
- Выбяры лепшы з `bel` / `bel_alt` або напішы свой, калі абодва дрэнныя.
- Крытэрыі: дакладнасць сэнсу адносна `en`; адпаведнасць glossary.md; натуральная літаратурная беларуская мова; адзінства тэрмінаў ва ўсім перакладзе.
- Тэгі і плэйсхолдэры бяры з `en` (у `bel_alt` яны часам сапсаваныя, напр. `<>` замест `<RT_BlueMed>`).
- Тэхнічныя ID (калі `en == ru == bel`, напр. `DLC06Fletcher1`) — капіраваць як ёсць.

## Крок 4. `ST_AltarDynamicTexts.json`, затым `ST_HardcodedContent.json`

Порцыі па ~100 запісаў. Тыя ж правілы. Гэта інтэрфейс: кароткія радкі не рабіць значна даўжэйшымі за `en`.

## Моўныя правілы

- Правапіс: афіцыйны (наркамаўка).
- Рэгістр: як у `en` для назваў прадметаў і пунктаў меню; уласныя імёны з вялікай літары; агульныя словы ў сярэдзіне фразы — з малой (не пераносіць англійскую звычку пісаць Кожнае Слова З Вялікай).
- Множны лік у фармаце ICU (`{Count}|plural(one=..., other=...)`): для беларускай мовы патрэбныя формы `one`, `few`, `many`, `other` (1 элемент, 2 элементы, 5 элементаў). Дадавай іх.
- Звяртанне да гульца — на «вы», як у існуючых радках.

## Парадак работы і прагрэс

- Пасля кожнай порцыі: `validate.py` → калі ёсць памылкі, выправіць → `git commit` з апісаннем (напр. `different: 3001–3300`) → адзначыць порцыю ў PROGRESS.md → адразу пераходзіць да наступнай.
- Калі сесія пачынаецца нанова — прачытай PROGRESS.md і працягвай з першага неадзначанага пункта.
- QUESTIONS.md: спрэчныя тэрміны і запісы (key, варыянты, што выбрана і чаму). Кароткае.
- У канцы: `validate.py` на ўсіх файлах і кароткая справаздача ў PROGRESS.md.
